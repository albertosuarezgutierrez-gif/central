## Resumen
<!-- Qué cambia y por qué, 2-4 líneas. El "por qué" pesa más que el "qué". -->

## Test plan
<!--
🚨 Checkbox marcado sin salida pegada = no verificado. "Ejecuté los tests" no es una prueba,
es una afirmación — la salida SÍ lo es. Pega el comando y su resultado real (recortado si es largo),
no un resumen. Ver skill `verification-before-completion`.
-->
- [ ] `pnpm exec tsc --noEmit` (o el típecheck de la app tocada) — salida:
- [ ] Tests relevantes — salida:
- [ ] Si toca UI: probado en el navegador (golpe de vista no vale) — captura o descripción de lo visto
- [ ] Responsive verificado si toca UI (≥320px)

## Pendiente
<!-- Solo si queda algo. Si no, borra esta sección. -->
