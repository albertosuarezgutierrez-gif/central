{
  "name": "casa-de-marcas",
  "version": "1.0.0",
  "private": true,
  "description": "Matriz de la casa de marcas (monorepo). La raíz NO es una vertical: las verticales viven en apps/* y los módulos compartidos en packages/*. Workspaces gestionados por pnpm (pnpm-workspace.yaml). Ver MATRIZ.md.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "auditar": "node scripts/auditar-estructura.mjs",
    "auditar:check": "node scripts/auditar-estructura.mjs --check",
    "test": "node --test test/*.test.ts && pnpm -r run test && pnpm run test:vitest",
    "test:guardia": "node --test test/*.test.ts",
    "test:packages": "pnpm -r --filter \"./packages/*\" run test",
    "test:vitest": "vitest run packages/core-firma packages/module-rrhh packages/module-documental packages/module-chat packages/module-transporte packages/core-identity/src/persona.test.ts packages/core-identity/src/secret.test.ts"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  },
  "pnpm": {
    "overrides": {
      "axios": ">=1.18.0",
      "sharp": ">=0.35.0",
      "postcss": ">=8.5.18",
      "uuid": ">=11.1.1",
      "vitest": ">=3.2.6 <4.0.0",
      "form-data": ">=4.0.6"
    },
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "esbuild",
      "@swc/core",
      "@parcel/watcher",
      "msedge-tts",
      "unrs-resolver"
    ]
  }
}
